# Production environment configuration
# This file is loaded automatically when running `npm run build`

# In production, the Svelte app is served by FastAPI as static files
# API requests are same-origin, so we use a relative path
VITE_API_BASE_URL=/api
